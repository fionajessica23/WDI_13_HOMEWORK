<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Hello World</title>
  <link rel='stylesheet' href='/stylesheets/main.css'>
</head>
<body>

  <header>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/desserts">All Desserts</a></li>
        <li>
        <% if logged_in? %>
          <a href="/logout">Logout</a>
        <% else %>
          <a href="/login">Login</a>
        <% end %>
        </li>
      </ul>
    </nav>
  </header>

  <% if logged_in? %>
    logged in as <%= current_user.email %>
  <%end%>

  <%= yield %>

</body>
